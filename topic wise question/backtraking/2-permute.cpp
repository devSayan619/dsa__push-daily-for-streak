#include<bits/stdc++.h>
using namespace std;

vector<string>findPer(string &s){
	vector<string>ans;
	permute
	return ans;
}

int main(){
	string s;
	cin>>s;

	vector<string>ans = findper(s);
	
	sort(ans.begin(),ans.end());

	for(auto i : ans){
		cout<<i<" ";
	}

	cout<<"\n";

	return 0;
}