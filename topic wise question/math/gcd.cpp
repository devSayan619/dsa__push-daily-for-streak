#include <bits/stdc++.h>
using namespace std;

int greaterElement(int a, int b)
{
  int ans = 0;
  return ans;
}

int main()
{
  int a, b;
  cin >> a;
  cin >> b;

  // int res = min(a, b);
  // while (res > 0)
  // {
  //   if (a % res == 0 && b % res == 0)
  //   {
  //     break;
  //   }
  //   res--;
  // }
  // cout << res << endl;

  cout << greaterElement(a, b) << endl;

  return 0;
}