#include<iostream>

using namespace std;
class Solution{
	public:
		bool check(vector<int>&nums){
		
		}
}

int main(){
	//check if the array is sorted and rootated not 
	vector<int>nums;
	bool result = check(nums);
	cout<<result<<endl;
	return 0;
}
